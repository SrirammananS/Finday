# LAKSH - Environment Variables Configuration Example
# Copy this file to .env and fill in your values

# 1. Google OAuth Client ID (Required)
# Create one at https://console.cloud.google.com/
# Add 'http://localhost:5173' to Authorized JavaScript origins
# Add 'http://localhost:5173/oauth-callback' to Authorized redirect URIs
VITE_GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com

# 2. App Identity
VITE_APP_NAME=LAKSH
VITE_APP_VERSION=1.0.32

# 3. Optional: IWA Configuration
# The domain where the app will be hosted (used for IWA packaging)
IWA_DOMAIN=https://your-app-domain.web.app
# Path to your private key for signing the web bundle
IWA_KEY=./iwa/private-key.pem

# 4. Storage Prefix (Optional)
# Change this if you want to avoid clashes with other apps on localhost
VITE_STORAGE_PREFIX=laksh_
